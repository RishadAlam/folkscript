<x-layout title="{{ __('Confirm it’s you') }}"><section class="page-shell auth-single"><h1>{{ __('A quick security check.') }}</h1><p class="muted">{{ __('Confirm your password to make changes to your account’s security.') }}</p><form method="post" action="{{ route('password.confirm') }}" class="stack">@csrf<label class="field">{{ __('Your password') }}<input class="form-input" type="password" name="password" autocomplete="current-password" required autofocus></label><button class="btn btn-primary" type="submit">{{ __('Confirm password') }}</button></form><a class="back-link" href="{{ route('settings') }}">{{ __('← Back to settings') }}</a></section></x-layout>
