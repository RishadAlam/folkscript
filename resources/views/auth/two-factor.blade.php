<x-layout title="{{ __('Verify your sign-in') }}"><section class="page-shell auth-single"><h1>{{ __('Just making sure it’s you.') }}</h1><p class="muted">{{ __('Enter the six-digit code from your authenticator app.') }}</p><form method="post" action="{{ route('two-factor.login') }}" class="stack">@csrf<label class="field">{{ __('Authentication code') }}<input class="form-input auth-code" name="code" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" autocomplete="one-time-code" autofocus></label><details class="auth-recovery"><summary>{{ __('Use a recovery code instead') }}</summary><label class="field">{{ __('Recovery code') }}<input class="form-input" name="recovery_code" autocomplete="off"></label></details><button class="btn btn-primary" type="submit">{{ __('Verify and sign in') }}</button></form><a class="back-link" href="{{ route('login') }}">{{ __('← Start over') }}</a></section></x-layout>
