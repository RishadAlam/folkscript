<x-layout title="{{ __('Choose a new password') }}"><section class="page-shell auth-single"><h1>{{ __('A fresh start.') }}</h1><p class="muted">{{ __('Choose a strong password you haven’t used before.') }}</p><form method="post" action="{{ route('password.update') }}" class="stack">@csrf<input type="hidden" name="token" value="{{ $token }}"><label class="field">{{ __('Email address') }}<input aria-invalid="{{ $errors->getBag('default')->has('email') ? 'true' : 'false' }}" aria-describedby="resetpassword1-error" class="form-input" type="email" name="email" value="{{ old('email', $email) }}" autocomplete="email" required><x-field-error name="email" bag="default" id="resetpassword1-error" /></label><label class="field">{{ __('New password') }}<input aria-invalid="{{ $errors->getBag('default')->has('password') ? 'true' : 'false' }}" aria-describedby="resetpassword2-help resetpassword2-error" class="form-input" type="password" name="password" minlength="10" autocomplete="new-password" required autofocus><span class="field-help" id="resetpassword2-help">{{ __('At least 10 characters, with a letter and number.') }}</span><x-field-error name="password" bag="default" id="resetpassword2-error" /></label><label class="field">{{ __('Confirm new password') }}<input aria-invalid="{{ $errors->getBag('default')->has('password_confirmation') ? 'true' : 'false' }}" aria-describedby="resetpassword3-error" class="form-input" type="password" name="password_confirmation" autocomplete="new-password" required><x-field-error name="password_confirmation" bag="default" id="resetpassword3-error" /></label><button class="btn btn-primary" type="submit">{{ __('Save new password') }}</button></form></section></x-layout>
