<x-layout title="{{ __('Choose a new password') }}"><section class="page-shell auth-single"><h1>{{ __('A fresh start.') }}</h1><p class="muted">{{ __('Choose a strong password you haven’t used before.') }}</p><form method="post" action="{{ route('password.update') }}" class="stack">@csrf<input type="hidden" name="token" value="{{ $token }}"><label class="field">{{ __('Email address') }}<input class="form-input" type="email" name="email" value="{{ old('email', $email) }}" autocomplete="email" required></label><label class="field">{{ __('New password') }}<input class="form-input" type="password" name="password" minlength="10" autocomplete="new-password" required autofocus><span class="field-help">{{ __('At least 10 characters, with a letter and number.') }}</span></label><label class="field">{{ __('Confirm new password') }}<input class="form-input" type="password" name="password_confirmation" autocomplete="new-password" required></label><button class="btn btn-primary" type="submit">{{ __('Save new password') }}</button></form></section></x-layout>
