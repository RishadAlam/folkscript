<x-layout title="{{ __('Verify your email') }}"><section class="page-shell auth-single"><div class="auth-mail-icon"><x-icon name="mail" /></div><h1>{{ __('One last thing.') }}<br>{{ __('Check your inbox.') }}</h1><p class="muted">{{ __('We sent a verification link to') }} <strong>{{ auth()->user()->email }}</strong>{{ __('. Verify your email to publish stories and become part of the conversation.') }}</p>@if(app()->environment('local') && in_array(config('mail.default'), ['log', 'array']))<p class="notice">{{ __('Email delivery is unavailable in this preview. You can sign in with an existing demo account.') }}</p>@endif<form method="post" action="{{ route('verification.send') }}" class="stack">@csrf<button class="btn btn-primary" type="submit">{{ __('Send a new verification link') }}</button></form><a class="back-link" href="/">{{ __('Keep exploring Folkscript →') }}</a></section></x-layout>
