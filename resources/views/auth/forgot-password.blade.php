<x-layout title="{{ __('Reset your password') }}"><section class="page-shell auth-single"><a class="back-link" href="{{ route('login') }}">{{ __('← Back to sign in') }}</a><h1>{{ __('Let’s get you back in.') }}</h1><p class="muted">{{ __('Enter the email address for your account. We’ll send you a link to choose a new password.') }}</p>@if(app()->environment('local') && in_array(config('mail.default'), ['log', 'array']))<p class="notice">{{ __('Email delivery is unavailable in this preview. You can sign in with an existing demo account.') }}</p>@endif<form method="post" action="{{ route('password.email') }}" class="stack">@csrf<label class="field">{{ __('Email address') }}<input aria-invalid="{{ $errors->getBag('default')->has('email') ? 'true' : 'false' }}" aria-describedby="forgotpassword1-error" class="form-input" type="email" name="email" value="{{ old('email') }}" autocomplete="email" required autofocus><x-field-error name="email" bag="default" id="forgotpassword1-error" /></label><button class="btn btn-primary" type="submit">{{ __('Send reset link') }}</button></form></section></x-layout>
