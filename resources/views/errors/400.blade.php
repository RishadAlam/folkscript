<x-layout :title="__('This link isn’t quite right.')">
<div class="page-shell error-page"><h1>{{ __('This link isn’t quite right.') }}</h1><p>{{ __('The search or page number could not be read. Start a new search to find what you’re looking for.') }}</p><div class="form-actions"><a class="btn btn-primary" href="/explore">{{ __('Search stories') }}</a><a class="text-link" href="/">{{ __('Home') }}</a></div></div>
</x-layout>
