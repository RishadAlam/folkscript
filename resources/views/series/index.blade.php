<x-layout title="{{ __('Your story collections') }}">
<div class="page-shell account-page">
    <div class="page-heading"><div><h1>{{ __('Stories that belong together.') }}</h1><p class="muted">{{ __('Build a series, explore a theme, or give your readers a place to begin.') }}</p></div><a href="/dashboard" class="btn btn-outline">{{ __('Back to your studio') }}</a></div>
    <nav class="tab-nav"><a href="/dashboard">{{ __('Your stories') }}</a><a href="{{ route('series.index') }}" class="active">{{ __('Collections') }} <span class="count">{{ $collections->count() }}</span></a></nav>
    <div class="settings-layout series-manager">
        <aside><nav class="settings-nav series-side-list" aria-label="{{ __('Your collections') }}">@forelse($collections as $collection)<a href="{{ route('series.edit', $collection) }}" @class(['active' => $selected?->id === $collection->id])><strong>{{ $collection->title }}</strong><span class="muted">{{ trans_choice('ui.story_count', $collection->posts_count) }}</span></a>@empty<p class="muted">{{ __('Your first collection begins here.') }}</p>@endforelse</nav><details class="settings-disclosure" @if(! $selected) open @endif><summary>{{ __('Create a collection') }}</summary><form method="post" action="{{ route('series.store') }}" class="stack">@csrf<label class="field">{{ __('Collection title') }}<input class="form-input" name="title" maxlength="100" placeholder="{{ __('A name for this chapter') }}" required></label><label class="field">{{ __('A short introduction') }}<textarea class="form-input" name="description" rows="3" maxlength="1000" placeholder="{{ __('What connects these stories?') }}"></textarea></label><button class="btn btn-primary" type="submit">{{ __('Create collection') }}</button></form></details></aside>
        <div class="settings-content">
            @if($selected)
                <section class="settings-section"><div class="section-heading"><div><h2>{{ $selected->title }}</h2><p class="muted">{{ __('By :name', ['name' => $selected->author->name]) }}</p></div><a class="btn btn-outline" href="{{ route('series.show', ['username' => $selected->author->username, 'slug' => $selected->slug]) }}">{{ __('View collection ↗') }}</a></div>
                    <form method="post" action="{{ route('series.update', $selected) }}" class="stack">@csrf @method('PATCH')<label class="field">{{ __('Title') }}<input class="form-input" name="title" value="{{ $selected->title }}" maxlength="100" required></label><label class="field">{{ __('Introduction') }}<textarea class="form-input" name="description" rows="3" maxlength="1000">{{ $selected->description }}</textarea></label><button class="btn btn-outline" type="submit">{{ __('Save details') }}</button></form>
                </section>
                <section class="settings-section"><h2>{{ __('A considered reading order.') }}</h2><p class="muted">{{ __('Set each story’s position, then save the order. Drafts and scheduled stories stay private until they are published.') }}</p>
                    @if($selected->posts->count())<form id="series-order" method="post" action="{{ route('series.order', $selected) }}">@csrf @method('PATCH')</form><div class="series-story-list">@foreach($selected->posts as $post)<div class="series-story-row"><label class="field"><span class="sr-only">{{ __('Position for :title', ['title' => $post->title]) }}</span><input form="series-order" type="number" name="positions[{{ $post->id }}]" min="1" max="1000" value="{{ $loop->iteration }}" class="form-input" required></label><div><a href="/write/{{ $post->id }}">{{ $post->title }}</a><span class="muted">{{ __('ui.status.'.$post->status) }} · {{ trans_choice('ui.reading_minutes', $post->reading_time) }}</span></div><form method="post" action="{{ route('series.posts.detach', ['series' => $selected, 'post' => $post]) }}">@csrf @method('DELETE')<button type="submit" class="text-button" aria-label="{{ __('Remove :title from collection', ['title' => $post->title]) }}">{{ __('Remove') }}</button></form></div>@endforeach</div><button form="series-order" class="btn btn-primary" type="submit">{{ __('Save reading order') }}</button>@else<div class="series-empty"><p>{{ __('No stories here yet. Add your first one below.') }}</p></div>@endif
                    @if($available->count())<form method="post" action="{{ route('series.posts.attach', $selected) }}" class="inline-form">@csrf<label class="field">{{ __('Add a story') }}<select class="form-input" name="post_id" required><option value="">{{ __('Choose one of your stories') }}</option>@foreach($available as $post)<option value="{{ $post->id }}">{{ $post->title }} ({{ __('ui.status.'.$post->status) }})</option>@endforeach</select></label><button class="btn btn-outline" type="submit">{{ __('Add story') }}</button></form>@else<p class="muted series-empty">{{ $selected->posts->count() ? __('All your available stories are in this collection.') : __('Write your first story, then add it to this collection.') }} <a href="/write">{{ __('Start a story →') }}</a></p>@endif
                </section>
                <details class="settings-disclosure"><summary>{{ __('Delete this collection') }}</summary><p class="muted">{{ __('The collection will be removed. Your stories will stay in your writing studio.') }}</p><form method="post" action="{{ route('series.destroy', $selected) }}">@csrf @method('DELETE')<button class="btn btn-outline" type="submit">{{ __('Delete collection') }}</button></form></details>
            @else<div class="empty-state"><x-icon name="book-open" size="36" /><h2>{{ __('Give your stories a shared home.') }}</h2><p>{{ __('Create a collection, add your stories, and arrange them in the order you want readers to discover them.') }}</p></div>@endif
        </div>
    </div>
</div>
</x-layout>
